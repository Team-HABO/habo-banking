services:
  service-auth:
    user: "0:0"
    build:
      target: dev
    env_file:
      - .env
    volumes:
      - ./service-auth:/src
      - /src/bin
      - /src/obj
    environment:
      - ASPNETCORE_ENVIRONMENT=${DOCKER_ENV:-Production}
      - DOTNET_USE_POLLING_FILE_WATCHER=1
