apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: rabbitmq-pvc
    namespace: habo-banking
spec:
    accessModes:
        - ReadWriteOnce
    resources:
        requests:
            storage: 1Gi
