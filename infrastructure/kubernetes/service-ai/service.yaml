apiVersion: v1
kind: Service
metadata:
  name: service-ai-metrics
  namespace: habo-banking
spec:
  selector:
    app: service-ai
  type: ClusterIP # Internal only — scraped by Prometheus
  ports:
    - port: 9091
      targetPort: 9091
