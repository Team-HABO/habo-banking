apiVersion: v1
kind: Service
metadata:
  name: service-account
  namespace: habo-banking
spec:
  selector:
    app: service-account
  type: LoadBalancer
  ports:
    - port: 8000
      targetPort: 8000
