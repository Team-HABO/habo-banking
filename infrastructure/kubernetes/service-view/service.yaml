apiVersion: v1
kind: Service
metadata:
  name: service-view
  namespace: habo-banking
spec:
  selector:
    app: service-view
  type: NodePort
  ports:
    - port: 4000
      targetPort: 4000
      nodePort: 30011 # Access at http://localhost:30011
