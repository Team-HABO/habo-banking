apiVersion: v1
kind: Service
metadata:
  name: service-notification-metrics
  namespace: habo-banking
spec:
  selector:
    app: service-notification
  type: ClusterIP # Internal only — scraped by Prometheus
  ports:
    - port: 9092
      targetPort: 9092
