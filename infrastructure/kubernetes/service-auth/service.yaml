apiVersion: v1
kind: Service
metadata:
  name: service-auth
  namespace: habo-banking
spec:
  selector:
    app: service-auth
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30010 # Access at http://localhost:30010
