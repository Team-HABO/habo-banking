apiVersion: apps/v1
kind: Deployment
metadata:
    name: postgresql-transaction
    namespace: habo-banking
spec:
    replicas: 1
    selector:
        matchLabels:
            app: postgresql-transaction
    strategy:
        type: Recreate # Required for stateful workloads with PVCs
    template:
        metadata:
            labels:
                app: postgresql-transaction
        spec:
            automountServiceAccountToken: false
            containers:
                - name: postgresql-transaction
                  image: postgres:15-alpine
                  imagePullPolicy: IfNotPresent
                  ports:
                      - containerPort: 5432
                  env:
                      - name: POSTGRES_USER
                        valueFrom:
                            secretKeyRef:
                                name: postgresql-transaction-secret
                                key: POSTGRES_USER
                      - name: POSTGRES_PASSWORD
                        valueFrom:
                            secretKeyRef:
                                name: postgresql-transaction-secret
                                key: POSTGRES_PASSWORD
                      - name: POSTGRES_DB
                        valueFrom:
                            secretKeyRef:
                                name: postgresql-transaction-secret
                                key: POSTGRES_DB
                  readinessProbe:
                      exec:
                          command: ['pg_isready', '-h', 'localhost', '-U', 'habouser']
                      initialDelaySeconds: 10
                      periodSeconds: 5
                      failureThreshold: 6
                  resources:
                      requests:
                          cpu: '100m'
                          memory: '256Mi'
                          ephemeral-storage: '256Mi'
                      limits:
                          cpu: '500m'
                          memory: '512Mi'
                          ephemeral-storage: '512Mi'
                  volumeMounts:
                      - name: postgresql-transaction-data
                        mountPath: /var/lib/postgresql/data
            volumes:
                - name: postgresql-transaction-data
                  persistentVolumeClaim:
                      claimName: postgresql-transaction-pvc
