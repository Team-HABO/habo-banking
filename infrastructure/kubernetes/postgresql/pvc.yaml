apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: postgresql-pvc
    namespace: habo-banking
spec:
    accessModes:
        - ReadWriteOnce
    resources:
        requests:
            storage: 2Gi
