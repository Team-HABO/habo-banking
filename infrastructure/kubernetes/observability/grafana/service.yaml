apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: habo-banking
spec:
  selector:
    app: grafana
  type: LoadBalancer
  ports:
    - port: 13000
      targetPort: 3000
      # Access at http://localhost:13000 (admin/admin) — Docker Desktop maps LoadBalancer to localhost
