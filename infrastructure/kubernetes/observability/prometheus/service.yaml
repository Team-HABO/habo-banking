apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: habo-banking
spec:
  selector:
    app: prometheus
  type: LoadBalancer
  ports:
    - port: 9090
      targetPort: 9090
      # Access at http://localhost:9090
