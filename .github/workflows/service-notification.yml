name: Service Notification CI

permissions:
  pull-requests: write

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - services/service-notification/**
  pull_request:
    paths:
      - services/service-notification/**

jobs:
  run-tests:
    uses: ./.github/workflows/service-notification-tests.yml
